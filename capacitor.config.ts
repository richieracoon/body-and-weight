import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'body-and-weight',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
